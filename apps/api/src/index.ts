/**
 * Ponto de entrada da API `@uilib/api`.
 *
 * Esta etapa cobre apenas a fundação do monorepo (workspaces, TypeScript,
 * lint, testes e scripts do Prisma). O bootstrap real do Express
 * (`server.ts`/`app.ts`), a validação de env (`config/env.ts`), as rotas e o
 * schema do banco (ver docs/MVP1.md, seções 4 a 9) ficam para as próximas
 * etapas de implementação do MVP1.
 */
console.log('[@uilib/api] fundação do monorepo — API ainda não implementada.');
