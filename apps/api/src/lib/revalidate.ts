/**
 * Placeholder: no MVP2, o frontend Next.js terá cache de página/dados que
 * precisa ser invalidado quando um componente ou categoria é criado/editado
 * via `/api/admin/*` (`revalidateTag`/`revalidatePath`). Esta etapa (MVP1)
 * não tem frontend nem cache — o módulo só reserva o local previsto na
 * estrutura do projeto (docs/MVP1.md, seção 4).
 */
export function revalidate(): void {
  // Implementado no MVP2, junto com o frontend Next.js.
}
